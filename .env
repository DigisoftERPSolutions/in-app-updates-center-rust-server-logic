BASE_URL = /pathogen/api/v1
DATABASE_URL = postgresql://bentito:Ben17872@194.163.165.74:5432/digiapp
JWT_SECRET = e8b818b1dabdf576d8acb82b744ce645b4fb4bf16d78bc30d7ea6c46b88258de
# Required by src/meter_ocr — the meter-photo cloud-OCR read. Fill in a
# real key before deploying; the endpoint 502s with a logged
# "GEMINI_API_KEY not set" until this is set.
GEMINI_API_KEY =

# Optional — overrides the default Gemini model string (see
# resolve_gemini_model() in operations.rs) without a redeploy, e.g. if
# Google renames/deprecates the built-in default. Unset = "gemini-2.5-flash".
GEMINI_MODEL =

# Optional overrides for src/meter_ocr's per-call cap and anomaly thresholds.
# Unset = use the in-code defaults (see operations.rs).
METER_OCR_DAILY_CAP_PER_COMPANY =
MAX_LITERS_DELTA_PER_SHIFT =
SALE_NUMBER_ROLLOVER_CEILING =